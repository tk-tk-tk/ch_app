- if user_signed_in?
  = simple_form_for([@messages, @messages.comments.build]) do |f|
    = f.input :content, label: "Comment"
    = f.button :submit
- else
  %p= flash[:success] = "Before using functionality, please log in."
