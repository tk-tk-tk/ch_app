= @messages.title
= @messages.description

= link_to "Back", root_path
= link_to "Edit", edit_message_path(@messages)
= link_to "Delete", message_path(@messages), method: :delete, data: { confirm: "Are you sure?" }
